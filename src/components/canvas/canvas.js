import React from "react";
import styles from "./canvas.module.css";

export default function Canvas() {
  return (
    <div className={styles["container"]}>
      Canvas should be here for getting user handwriting
    </div>
  );
}
